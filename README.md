# MailerService (Open Source)
Serviço backend de envio de e-mails com API + Worker, observabilidade, segurança (ASVS),
e integração com provedores (SMTP, SendGrid, SES). Siga docs/ para guias.

## Dev rápido
docker compose up -d
dotnet build
